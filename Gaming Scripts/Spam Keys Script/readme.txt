Spam Keys Script;

Makes it so that the user can hold down a key or a mouse button to keep sending that key.
When the user stops holding the key, the script will stop sending keys.
Toggleable with CTRL+L.

We can add different keys in the source file to spam different keys.
